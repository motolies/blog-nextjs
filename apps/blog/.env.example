# 로컬 개발 예시 — 복사해서 .env.local 로 쓴다.
# true 면 Next 서버가 hvy-blog 백엔드 대신 MSW 목(mocks/)에 응답을 받는다.
# ⚠️ mocks/ 수정 시 dev 서버 재시작 필수 (instrumentation 은 프로세스당 1회).
# ⚠️ 목에 없는 경로는 bypass 로 실 백엔드(api.hvy.kr)로 나간다.
MOCK_DOWNSTREAM=true
